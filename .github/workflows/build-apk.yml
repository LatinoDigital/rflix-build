name: Build RFlix APK

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: false
        default: 'v591'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Runner started successfully"
